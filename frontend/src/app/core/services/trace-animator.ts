// Replaced by real SSE streaming in ReviewService.streamDiff().
// The seam described in the README has been wired up — agent events now
// flow from the backend in real time rather than a scripted animation.
export {};
